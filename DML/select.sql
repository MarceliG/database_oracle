SELECT
  BUILDING_ID,
  BUILDING_NAME,
  STREET,
  STREET_NUMBER,
  ZIP_CODE,
  CITY,
  COUNTRY,
  FLOORS_NUMBER
FROM
  BUILDING;

SELECT
  COMPUTER_ID,
  ROOM_ID,
  COMPUTER_NAME,
  SERIAL_NUMBER,
  MANUFACTURER,
  TYPE,
  CPU_ID,
  GPU_ID,
  RAM,
  WARRANTY_DATE,
  CONNECTORS,
  DESCRIPTION
FROM
  COMPUTER;

SELECT
  CPU_ID,
  CPU_NAME,
  NUMBER_OF_THREADS,
  FREQUENCY,
  COMPUTERGPU
FROM
  CPU;

SELECT
  GPU_ID,
  GPU_NAME,
  MEMORY
FROM
  GPU;

SELECT
  COMPUTER_ID,
  HDD_ID,
  HDD_NAME,
  MEMORY
FROM
  HDD;

SELECT
  MONITOR_ID,
  ROOM_ID,
  COMPUTER_ID,
  MANUFACTURER,
  SERIAL_NUMBER,
  DIAGONAL,
  RESOLUTION,
  REFRESH_RATE,
  WARRANTY_DATE,
  COMPUTERCPU_ID,
  COMPUTERGPU
FROM
  MONITOR;

SELECT
  PRINTER_ID,
  ROOM_ID,
  PRINTER_NAME,
  MANUFACTURER,
  SERIAL_NUMBER,
  COLOR_PRINTING,
  SCANNER,
  WARRANTY_DATE
FROM
  PRINTER;

SELECT
  COMPUTER_ID,
  RAM_ID,
  RAM_NAME,
  SPEED
FROM
  RAM;

SELECT
  ROOM_ID,
  BUILDING_ID,
  ROOM_NAME,
  FLOOR,
  ROOM_NUMBER
FROM
  ROOM;

SELECT
  SOFTWARE_ID,
  COMPUTER_ID,
  SOFTWARE_NAME,
  MANUFACTURER,
  LICENSE_KEY,
  EXPIRE_DATE,
  COMPUTERCPU_ID,
  COMPUTERGPU
FROM
  SOFTWARE;

SELECT
  COMPUTER_ID,
  SSD_ID,
  SSD_NAME,
  MEMORY
FROM
  SSD;

SELECT
  USER_ID,
  LOGIN,
  PASSWORD,
  EMAIL,
  FIRST_NAME,
  SURNAME,
  LAST_LOGIN
FROM
  "user";

SELECT
  USER_ID,
  BUILDING_ID
FROM
  USER_BUILDING;